# 六个基本三角函数的定义

## 核心概念

三角函数最初基于直角三角形定义，后通过单位圆推广至任意角。

- **直角三角形定义**：适用于锐角（0° 到 90°）
- **单位圆定义**：适用于任意角度，更为通用

在单位圆（半径为 1 的圆）中，角度 θ 的终边与圆交于点 P(x, y)，该点的坐标定义了各个三角函数。

## 函数定义表

| 函数名称 | 符号 | 单位圆定义<br>(点 P(x, y)) | 直角三角形定义<br>(角 θ) | 倒数关系 |
| :--- | :--- | :--- | :--- | :--- |
| **正弦**<br>(Sine) | sin | y | **对边 / 斜边** |  |
| **余弦**<br>(Cosine) | cos | x | **邻边 / 斜边** |  |
| **正切**<br>(Tangent) | tan | y / x | **对边 / 邻边** |  |
| **余切**<br>(Cotangent) | cot | x / y | **邻边 / 对边** | cot(θ) = 1/tan(θ) |
| **正割**<br>(Secant) | sec | 1 / x | **斜边 / 邻边** | sec(θ) = 1/cos(θ) |
| **余割**<br>(Cosecant) | csc | 1 / y | **斜边 / 对边** | csc(θ) = 1/sin(θ) |

## 补充说明

### 1. 定义域与值域

- **sin(θ)** 和 **cos(θ)**：定义域为所有实数，值域为 [-1, 1]
- **tan(θ)** 和 **sec(θ)**：当 cos(θ) = 0 时未定义
- **cot(θ)** 和 **csc(θ)**：当 sin(θ) = 0 时未定义

### 2. 函数关系

- **商数关系**：tan(θ) = sin(θ)/cos(θ)，cot(θ) = cos(θ)/sin(θ)
- **平方关系**：sin²(θ) + cos²(θ) = 1，1 + tan²(θ) = sec²(θ)，1 + cot²(θ) = csc²(θ)

### 3. 历史符号注记

您可能熟悉的旧符号体系：
- **余切**函数可能记为 **ctg** (现通用 **cot**)
- **正割**和**余割**可能未学习或使用不同符号

现代符号更加系统化且与国际标准接轨，但函数的基本数学关系保持不变。


# 三角函数积分公式大全

## 基本三角函数积分

| 序号 | 函数 | 积分公式 | 备注 |
|:---:|:---:|:---|:---|
| 1 | $\sin(x)$ | $\int \sin(x)  dx = -\cos(x) + C$ |  |
| 2 | $\cos(x)$ | $\int \cos(x)  dx = \sin(x) + C$ |  |
| 3 | $\tan(x)$ | $\int \tan(x)  dx = -\ln \vert \cos(x) \vert + C$ | 也常写作 $\ln \vert \sec(x) \vert + C$ |
| 4 | $\cot(x)$ | $\int \cot(x)  dx = \ln \vert \sin(x) \vert + C$ | 也常写作 $-\ln \vert \csc(x) \vert + C$ |
| 5 | $\sec(x)$ | $\int \sec(x)  dx = \ln \vert \sec(x) + \tan(x) \vert + C$ | 这是一个需要记忆的公式 |
| 6 | $\csc(x)$ | $\int \csc(x)  dx = -\ln \vert \csc(x) + \cot(x) \vert + C$ | 也是一个需要记忆的公式 |

## 平方函数的积分

| 序号 | 函数 | 积分公式 |
|:---:|:---:|:---|
| 7 | $\sin^2(x)$ | $\int \sin^2(x)  dx = \frac{x}{2} - \frac{\sin(2x)}{4} + C$ |
| 8 | $\cos^2(x)$ | $\int \cos^2(x)  dx = \frac{x}{2} + \frac{\sin(2x)}{4} + C$ |
| 9 | $\tan^2(x)$ | $\int \tan^2(x)  dx = \tan(x) - x + C$ | (利用 $1 + \tan^2(x) = \sec^2(x)$) |
| 10 | $\sec^2(x)$ | $\int \sec^2(x)  dx = \tan(x) + C$ | (常见导数逆运算) |
| 11 | $\csc^2(x)$ | $\int \csc^2(x)  dx = -\cot(x) + C$ | (常见导数逆运算) |
| 12 | $\cot^2(x)$ | $\int \cot^2(x)  dx = -\cot(x) - x + C$ | (利用 $\cot^2(x) = \csc^2(x) - 1$) |

## 乘积的积分（积化和差）

| 序号 | 函数 | 积分公式（应用积化和差后） |
|:---:|:---:|:---|
| 13 | $\sin(mx)\sin(nx)$ | $\int \sin(mx)\sin(nx)  dx = \frac{\sin((m-n)x)}{2(m-n)} - \frac{\sin((m+n)x)}{2(m+n)} + C$ $(m \neq n)$ |
| 14 | $\cos(mx)\cos(nx)$ | $\int \cos(mx)\cos(nx)  dx = \frac{\sin((m-n)x)}{2(m-n)} + \frac{\sin((m+n)x)}{2(m+n)} + C$ $(m \neq n)$ |
| 15 | $\sin(mx)\cos(nx)$ | $\int \sin(mx)\cos(nx)  dx = -\frac{\cos((m-n)x)}{2(m-n)} - \frac{\cos((m+n)x)}{2(m+n)} + C$ $(m \neq n)$ |

## 重要技巧与说明

### 1. 降幂公式
积分 $\sin^n(x)$ 或 $\cos^n(x)$（n 为正整数）时：
- 如果 n 是奇数，常用凑微分法（如 $\sin^3(x) = \sin^2(x) \cdot \sin(x) = (1-\cos^2(x)) \cdot \sin(x)$）
- 如果 n 是偶数，则必须使用降幂公式（如 $\sin^2(x) = \frac{1-\cos(2x)}{2}$）逐次降低幂次

### 2. 万能公式代换
对于由 $\sin(x)$ 和 $\cos(x)$ 构成的复杂有理分式积分，有时会使用代换 $t = \tan(\frac{x}{2})$（称为万能公式），将三角积分转化为有理函数积分。

### 3. sec(x) 和 tan(x) 的高次幂
积分 $\sec^n(x)\tan^m(x)$ 有固定的处理策略：
- 如果 m 是奇数，从 $\tan(x)$ 中分出一个 $\sec(x)\tan(x)$ 来凑微分
- 如果 n 是偶数，从 $\sec^2(x)$ 中分出一个 $\sec^2(x)$ 来凑微分

### 4. 分部积分法
三角函数常与 $e^x$ 或 $x^n$ 结合出现在积分中（如 $\int e^x \sin(x)  dx$），这类积分通常需要使用**分部积分法**。